<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
    >

    <!--<ProgressBar
        android:id="@+id/progressBarToday"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="250dp"
        android:layout_height="273dp"
        android:layout_centerInParent="true"
        android:indeterminate="false"
        android:progressDrawable="@drawable/circular_progress_bar" />
    <TextView
        android:id="@+id/timer_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        />-->

    <ru.extremefitness.fitness_trainer.view.WorkoutTimer
        android:id="@+id/workout_timer"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:timerColor="@color/ef_yellow"
        app:arcColor="@color/ef_grey"
        app:max="60"
        app:progress="60"
        app:pointerColor="@color/ef_lavender"
        android:layout_gravity="center"
        android:background="@color/ef_lavender"
        />
</LinearLayout>